@model GetAllSuppliersVm
@{
    ViewData["Title"] = "GetAllSuppliers";
}
@{
    var prevPage = Model.CurrentPage - 1;
    var nextPage = Model.CurrentPage + 1;
}
<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <div class="row align-items-center justify-content-between">
                    <div class="col-lg-6">
                        <h2 class="card-title">All Suppliers</h2>
                    </div>
                    <div class="col-lg-2">
                        <div class="btn-group" role="group" aria-label="Basic example">
                            <a class="btn btn-outline-secondary" asp-action="GetAllUsers" asp-route-pageSize="@Model.PageSize"> &lt;&lt; </a>
                            @if (prevPage > 0)
                            {
                                <a class="btn btn-outline-secondary" asp-action="GetAllUsers" asp-route-page="@prevPage" asp-route-pageSize="@Model.PageSize">@prevPage</a>
                            }

                            <a class="btn btn-primary active" style="color:honeydew">@Model.CurrentPage</a>

                            @if (nextPage <= Model.TotalPages)
                            {
                                <a class="btn btn-outline-secondary" asp-action="GetAllUsers" asp-route-page="@nextPage" asp-route-pageSize="@Model.PageSize">@nextPage</a>
                            }
                            <a class="btn btn-outline-secondary" asp-action="GetAllUsers" asp-route-page="@Model.TotalPages" asp-route-pageSize="@Model.PageSize"> &gt;&gt; </a>
                        </div>
                    </div>

                    <div class="col-lg-2">
                        <div class="d-flex flex-column">

                            <select class="form-control form-control" id="pageSize" style="color:black">
                                @foreach (var size in Model.PageSizes)
                                {
                                    @if (Model.PageSize == size)
                                    {
                                        <option selected>@size</option>
                                        continue;
                                    }
                                    <option>@size</option>
                                }
                            </select>
                        </div>
                    </div>

                </div>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                @foreach (var propertyInfo in typeof(AllSuppliersDto).GetProperties())
                                {
                                    if (propertyInfo.Name == "Id")
                                    {
                                        continue;
                                    }
                                    <th>
                                        @propertyInfo.Name
                                    </th>
                                }
                                <th>
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var supplier in Model.Suppliers)
                            {
                                <tr>
                                    @foreach (var prop in supplier.GetType().GetProperties())
                                    {
                                        switch (prop.Name)
                                        {
                                            case "Id":
                                                continue;
                                            case "CompanyName":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "ContactName":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "ContactTitle":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "Address":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "City":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "Region":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "PostalCode":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "Country":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "PhoneNumber":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "Fax":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "Website":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            case "UpdatedAt":
                                                <td class="py-1">
                                                    @prop.GetValue(supplier)
                                                </td>
                                                continue;
                                            default:
                                                <td>
                                                    @prop.GetValue(supplier)
                                                </td>
                                                break;
                                        }
                                    }
                                    <td>
                                        <a asp-action="Update" asp-controller="Suppliers" asp-route-Id="@supplier.Id" class="btn btn-success btn-icon-text">
                                            Update
                                            <i class="mdi mdi-pencil-circle btn-icon-append"></i>
                                        </a>
                                        <form method="post" asp-action="AssumingDeleted" asp-controller="Suppliers" class="m-auto">
                                            <input name="Id" id="Id" hidden value="@supplier.Id" />
                                            <button type="submit" class="btn btn-warning btn-icon-text">
                                                Assuming deleted
                                                <i class="mdi mdi-delete btn-icon-append"></i>
                                            </button>
                                        </form>
                                        <form method="post" asp-action="Delete" asp-controller="Suppliers" class="m-auto">
                                            <input name="Id" id="Id" hidden value="@supplier.Id" />
                                            <button type="submit" class="btn btn-danger btn-icon-text">
                                                Really delete
                                                <i class="mdi mdi-delete btn-icon-append"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section GetAllUsersJs {
    <script src="~/admin/js/users-paginate.js"></script>
}